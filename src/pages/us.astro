---
import Html from "@components/Html.astro";
import Map from "@components/maps/places";
import data from "@data/us.json";

const frontmatter = {
  title: "Us",
  description: "Map of all places we've visited.",
  authors: [{ collection: "authors", id: "sk" }],
  date: new Date(),
};
---

<Html frontmatter={frontmatter}>
  <Map
    client:only="react"
    token={import.meta.env.PUBLIC_MAPBOX_ACCESS_TOKEN}
    places={data}
    zoom={12}
  />
</Html>
